spring.application.name=shahimart

#debug=true

logging.pattern.console=%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n
logging.level.root=INFO
logging.level.com.shahimart.shahimart.controller=DEBUG

logging.file.name=/Users/princeshahi/Documents/fullstack-react-springboot/section5/shahimart/log/shahimart_log.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

cors.allowed-origins=${SHAHIMART_CROSS_ORIGIN:http://localhost:5174}
#spring.datasource.url=jdbc:h2:file:~/shahimart;AUTO_SERVER=true
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=optional:classpath:sql/schema.sql
#spring.sql.init.data-locations=optional:classpath:sql/data.sql
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.show-sql=true

#
#spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:shahimart}
#spring.datasource.username=${DATABASE_USERNAME:root}
#spring.datasource.password=${DATABASE_PASSWORD:root}
#spring.jpa.show-sql=${JPA_SHOW_URL:true}
#spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}


spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:shahimart}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Prince@123}

spring.jpa.show-sql=${JPA_SHOW_URL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Show full health details
management.endpoint.health.show-details=always
# Enable all endpoints
management.endpoints.web.exposure.include=*
# Change base path (default is /actuator)
management.endpoints.web.base-path=/shahimart/actuator



management.endpoint.info.enabled=true
management.info.env.enabled=true
info.app.name=${spring.application.name}
info.app.description=ShahiMart is a scalable e-commerce platform built to deliver a \
  smooth and efficient online shopping experience. It supports product browsing, \
  secure transactions, order management, and responsive design, ensuring accessibility across devices.
info.app.version=1.0.0
info.app.email=shahip008@gmail.com

